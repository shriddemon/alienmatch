import React from "react";
import ReactDOM from "react-dom";
import AlienMatch from "./AlienMatch";
import "./App.css";

ReactDOM.render(
  <React.StrictMode>
    <AlienMatch />
  </React.StrictMode>,
  document.getElementById("root")
);
